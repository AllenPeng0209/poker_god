# poker_god_api (migration scaffold)

This folder is the target backend home under `services/<project>/`.

Current runtime app remains in `services/api/app/main.py`.

Migration step introduced in this run:
- Added conversion blockers API contract in `services/api`.
- Added this scaffold to progressively move backend code into `services/poker_god_api` while keeping compatibility.
